请在当前仓库实现一个“船厂空间布局编辑器”，而不是仅能画图并导出画布 JSON 的演示网页。

先读取并遵守 AGENTS.md、docs/PRODUCT_SPEC.md、docs/IMPLEMENTATION_PLAN.md 和 docs/ACCEPTANCE_TESTS.md。若已有代码或规则，先检查并说明兼容方案，不覆盖已有工作。

目标：我手工绘制或参照带尺度底图设计多套船厂地图，保存成可由人和 Codex 直接修改的 JSON；地图应服务于具有真实空间依据的二维运输仿真，并为将来的三维数字孪生、VR 和外部调度器保留稳定接口。

采用模块化浏览器应用，默认 TypeScript 严格模式、React、Vite、Konva/react-konva。若仓库已有等效技术栈，可保留并记录理由。HTML 是浏览器交付方式，不要求把所有代码塞进单个 HTML 文件。依赖锁定到实际验证的版本。

领域数据必须与 React、Canvas、Konva、Three.js 等完全解耦。地图以本地米制右手坐标系保存，XY 为地面、Z 向上；角度使用弧度，质量使用千克，时间使用秒。像素缩放和平移仅属于视图。

JSON Schema 与语义校验先行。地图包含节点、道路折点、设施边界、出入口/服务点、作业区、路口连接、资源区、来源及底图变换。使用稳定 ID，名称可更改。道路几何、长度和派生图只有明确的数据来源，不保存互相冲突的可编辑副本。未来扩展只能放入声明的命名空间并完整保留。

道路相交不自动连通；只有明确共享节点和转向连接才表示可通行。设施面积不等于车辆入口，设施中心不自动成为装卸点。道路外观、颜色和线宽不决定物理属性。不把所有道路设为40米，也不把全部路口默认设成单车独占。

必须实现本地 JSON 导入导出、事务化编辑、撤销重做、结构化报错和无浏览器的地图验证命令。所有界面编辑通过领域命令执行；Codex 外部修改 JSON 后经过同一校验、派生和导入路径重建地图。外部修改与未保存的浏览器编辑发生冲突时不得静默覆盖。

静态 map.json、scenario.json、运行日志/回放、editor-state.json 分开。纯 JSON 包含所有语义几何；底图等二进制资源使用 assets 相对路径，完整工程以 zip 打包。丢失底图时应仍可查看和校验矢量地图，而不是丢失厂区模型。

首版仅做二维编辑与网络级校验/路径预览。预留框架无关 SceneSnapshot、SimulationAdapter 和 RenderAdapter；不要提前实现 VR、物理引擎或复杂优化器，也不要用空函数声称已经支持。

现在先完成 M0+M1：
M0：检查仓库，提交架构决策、初版 JSON Schema、字段/坐标文档、测试计划；明确未决假设。
M1：实现“新建地图→米制画布→节点与道路折线→数值编辑→撤销重做→稳定 JSON 导出→外部修改 JSON→重新导入→CLI 校验”的可运行闭环。提供一个明确标为 synthetic 的示例和一个可定位错误的无效样例。

不要只写计划。请在计划后实际完成 M1、运行类型检查/单元测试/浏览器关键路径测试，并报告具体命令和结果。环境无法运行的检查要写清楚，不能声称通过。完成后列出已实现、未实现、已知限制和下一阶段任务；本次不要提前扩展到 M2—M5。
