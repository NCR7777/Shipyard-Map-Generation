# 项目工程规则

## 任务与范围

本项目是可持续开发的船厂空间布局编辑器。阅读 docs/PRODUCT_SPEC.md、docs/IMPLEMENTATION_PLAN.md 和 docs/ACCEPTANCE_TESTS.md。按当前明确指定的里程碑实现，不把需求计划写成已完成功能。已有仓库先检查再改动；不覆盖用户已有文件。

## 领域模型优先

- 权威数据是版本化 YardMap JSON，不是画布场景树、DOM、Konva/Fabric 序列化或 Three.js Scene。
- src/domain、src/geometry、src/topology、src/compiler、src/validation 不得导入浏览器 DOM、React、Konva 或三维引擎。
- 世界坐标统一：本地米制右手坐标；XY 地面、Z 向上；角度 rad、时间 s、质量 kg。屏幕 Y 向下的转换只发生在适配器。
- 节点位置为权威；道路中心线由节点位置和内部折点推导。派生长度、包围盒、双向弧与可视网格不得成为可冲突的第二份主数据。
- ID 稳定且与显示名称分离；修改名称、移动对象不能重建其 ID。复制对象要生成新 ID 并原子重映射内部引用。
- 仅以显式引用定义连通。相交、接近、重叠均不能静默等价于连接。
- 颜色、图层顺序、隐藏状态不得改变通行或资源占用语义。

## JSON 与兼容性

- 明确 JSON Schema 版本、schemaVersion、mapId、文档 revision。Schema 是结构契约，不能代替跨实体、拓扑及几何校验。
- 定义有限数值、单位、枚举、未知值、无限制和不适用的不同语义；不能把 null 当作0或无限。
- 核心对象采用严格字段；命名空间 extensions 用于扩展。不能静默丢弃未来字段或语义。
- 不支持的版本只读或拒绝；迁移显式执行、保留原件并输出差异。
- 对象键按固定顺序序列化；数组中几何顶点次序保留。禁止导出时反复舍入导致累积误差。
- 内容摘要由规范化后的声明数据计算；不能只信任手填 revision 或 updatedAt。定义摘要是否包含来源、底图及视觉属性。
- 外部 JSON 与当前未保存编辑冲突时提示，并允许重载或另存为；不得自动覆盖。

## 编辑与校验

- 所有业务编辑通过可测试、事务化的命令接口；拖拽在结束时提交一个可撤销事务。
- 删除、道路拆分、节点合并和设施整体移动都要更新引用或拒绝操作，不能生成孤立引用。
- 失败导入不替换当前有效地图；错误包含 code、severity、entityId/JSON路径和定位信息。
- 草稿允许保存；发布给仿真需要通过所声明配置的校验。不能把“语法有效”或“动画可播”标为“现实运输安全”。
- 渲染缓存及异步计算必须标注地图摘要；旧版本结果不得覆盖新地图。

## 工程实现

- 默认 TypeScript strict + React + Vite + Konva/react-konva；现有等效栈可以保留，但需架构记录。选择一套二维渲染器，不重复建设多套UI。
- 首期单仓库模块化应用，不建设微服务、用户系统或云数据库。需要提取包时再提取。
- 几何运算必须使用并验证平面米制算法，不能把米制坐标传给默认经纬度/球面计算的函数。
- 核心数据、命令、验证和编译必须可在 Node 环境无界面运行。
- 浏览器输入、JSON、图片和压缩包视为不可信输入；限制大小与解压总量、阻止路径穿越。首版只接受声明的栅格底图格式，不直接执行 SVG/HTML 内容。
- 本地优先；未经用户授权不上传地图、底图或日志，不加入遥测。
- 保存使用标准文件导入下载作为基线，文件系统选择器为可用时的增强；不假设能静默访问任意本地文件。
- 重计算进入 Worker 时保留纯计算接口、任务ID、取消或丢弃过期响应策略。

## 验证与交付

以实际 package.json 为准维护开发命令；M0/M1 建立并执行至少：安装锁定依赖、dev、build、typecheck、lint、test、test:e2e、map:validate。编译功能实现后增加 map:compile。

每次数据契约变更必须同步 Schema、类型、迁移说明、示例与回归测试。新增功能有对应验收测试，不通过修改或删除失败测试掩盖问题。

报告实际执行的命令与结果，区分通过、失败和未运行。没有实现的适配器应返回明确 unsupported，不得返回伪成功。保留未解决风险和下一步清单。

## 数据真实性

手绘/合成布局标 synthetic 或 conceptual；有底图支撑但未独立核验标 reference_based；不得自动标 surveyed/verified。未知道路宽度、高度、重量限制和资源容量不填虚构实测值。数值假设属于显式、可审查的假设配置。

运行状态、任务时刻、事件和性能结果不写回静态地图。地图变更后旧仿真结果仍绑定旧地图摘要，不随新图改写历史。

## 当前产品边界与长期 Ponytail 规则（合并，不覆盖原文件）

本项目只做轻量船厂拓扑/空间地图绘制、服务点标注、保存与JSON交换，是后续系统的数据准备工具。停止执行历史文档中在本仓库建设装卸仿真、调度平台、3D/VR的路线；除非用户以后明确重新授权这些功能。保留已有数据与接口，不擅自删除。

所有后续编码/修复/重构先读取 `.agents/skills/ponytail/SKILL.md`，采用full；提交前读取 `.agents/skills/ponytail-review/SKILL.md` 检查冗余，并另外检查正确性。技能不存在或未成功加载时如实说明。

优先不做不必要的功能，再复用本仓库、标准库、原生能力和已安装依赖；最后才写最小自定义代码。Ponytail是开发工具，不加入应用运行依赖。不上新框架、不做推测性平台、不为凑行数压缩代码。

本仓库明确要求保留保存/恢复、校验、引用一致性、输入保护和现有回归测试；不得以技能中的简化建议删除这些已要求的保护。业务数据和核心算法不依赖画布，控制柄属于界面而非路网节点。

矩形拖角必须保持四个直角与对角锚点，默认宽高可独立变化，不默认锁定长宽比；旋转后的矩形按本地轴缩放。权威几何仍为米制边界，临时画布变换不进入JSON；单次拖动单次事务。

只维护必要文档和实际package.json命令。报告真正执行的测试；未运行、未安装、未实现必须区分。未经确认不全仓重写、不破坏旧地图、不覆盖用户改动。
