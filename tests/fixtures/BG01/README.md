# BG01 synthetic raster fixtures

These tiny, synthetic 3 x 2 PNG, JPEG and WebP images exercise format admission and actual browser decoding. `rotated-exif.jpg` declares EXIF orientation 6 for normalization/rejection testing. They contain no shipyard imagery, coordinates or field evidence. Real-map tests load separately frozen local inputs and fail with `blocked_input` when unavailable.
