# 本轮任务：Ponytail 精简重构 + 画布直接编辑

审查基线：NCR7777/Shipyard-Map-Generation 的 89044af6cfe97f78c5191dc194805adf22c90b7a。
本文件是待执行开发任务，不是已完成的软件或测试报告。远程仓库未被本次审查修改。

## 目标与停止范围

项目仅是轻量船厂拓扑地图绘制器和外部系统的数据准备工具。
本轮实现“选择设施/区域 → 显示控制柄 → 拖动修改形状/尺寸 → 撤销 → 保存重开”。
停止按历史 M3A 计划在本仓库开发装卸事件引擎、调度、资源竞争、任务回放、3D、VR。
保留已有服务点及装卸语义标签、JSON、稳定ID和接入接口；真正执行操作属于外部系统。
保存、导入保护、引用校验、基础几何校验、键盘与数值编辑不属于可随意删除的累赘。

## 先使用 Ponytail，再改代码

1. 读取 .agents/skills/ponytail/SKILL.md 与 .agents/skills/ponytail-review/SKILL.md，采用 full。
2. 把 AGENTS_APPEND.md 的长期规则合并到现有 AGENTS.md；整理与新范围冲突的旧里程碑指令，不整体覆盖原文件。
3. 核对当前分支、提交和未提交改动；基线变化时先读差异，不覆盖用户工作。
4. 先追踪本次修改的真实调用链，再用 ponytail-review 给出少量有代码位置的简化建议，随后实际实施。
5. Ponytail 是开发技能，不是运行库，不加入应用 dependencies，不加载到网页，不抄其整个仓库或基准测试。
6. 已有同名技能时核对来源/版本并合并；不要并行安装多份或自动更新。当前副本信息见 UPSTREAM.md。

## 已核实的代码问题

- src/renderers/2d/SpatialLayer.tsx：设施/区域目前是 Group + Shape，可整体拖动，但没有尺寸控制柄、顶点拖动或 Transformer。
- src/renderers/2d/useSpatialDrawing.ts：矩形绘制最终生成普通 Polygon；工厂函数只保留 boundary，没有矩形专用编辑规则。
- src/ui/SpatialPropertyPanel.tsx：当前逐顶点XYZ编辑可把矩形变成合法但不规则的多边形；一般多边形合法性不等于矩形约束。
- src/domain/commands.ts：已有 updateFacility/updateZone、平移、旋转、复制、删除；优先复用提交、验证和历史流程，不再引入第二套编辑状态引擎。
- src/ui/App.tsx 重复声明 selectionKinds，核心已导出 SELECTION_KINDS；确认调用后直接复用。其保存、文件冲突和弹窗代码混合较多，只按职责做必要的局部整理，不做全仓改写。
- 0.1.0/0.2.0 已有双版本支持，不应仅为了四个编辑控制点就强制升级地图格式。

## A. 矩形交互：必须保持矩形，不默认锁定长宽比

- 单选矩形设施或矩形区域后，出现四角控制柄；已有组件能低成本支持时可保留四个边中点。
- 拖动一个角：对角固定，宽高由指针在原矩形局部坐标系中的两个分量共同决定，另外两角联动。
- 默认允许60×30变为80×45；不能强制只能80×40。“保持矩形”与“锁定长宽比”不同。
- 有边中点时，只改变对应一个尺寸；Shift等比属于可选便利功能，不是本次核心门槛。
- 已旋转矩形也必须保持四个直角。不得对世界坐标中的四角做错误的非均匀缩放导致平行四边形。
- 采用明确的正尺寸下限，拒绝或夹住越过对角线的拖动，不产生负宽高、翻转和零面积。下限用世界米制，不能依赖缩放后的像素。
- 实时显示矩形本地宽、高；不要把旋转后的世界轴对齐包围盒尺寸当作矩形本身长宽。
- 矩形模式的数值编辑也走同一几何规则；默认不开放任意单角XYZ编辑。自由变形需用户明确选择多边形编辑模式。

### 最小数据实现

继续以 boundary.outer/holes 为权威几何。选择对象时，从无孔洞、四个不同顶点加闭合点的合法多边形中验证并提取矩形框架（共面、正长度、相邻垂直、对边一致），不要把任意四边形当矩形。
原矩形边的局部方向在一次拖动内冻结。交互结束生成新的四角和闭合点，提交现有 updateFacility/updateZone。
尺寸、中心、角度可以从边界派生并暂存于交互层，不重复持久化一套可冲突的参数。
输入本来是不规则四边形、有孔洞或无法可靠识别矩形时，保留其真实几何，使用多边形编辑，绝不静默“修正”为矩形。
矩形编辑模式只需要当前会话选择；重新导入可从实际几何重新识别，不为此新增 schemaVersion。

### 复用已安装的 Konva

优先使用现有 react-konva 的 Rect/Transformer，不新增 Fabric、另一套CAD/编辑器框架或几何约束求解器。
Transformer 可提供控制柄，但保持矩形、坐标变换、领域提交与撤销仍需正确接入。
默认 keepRatio=false；禁止翻转。Transformer 修改的是 scaleX/scaleY，结束时烘焙为世界坐标边界，重置临时scale。
当前 SpatialLayer 把世界坐标预投影为屏幕坐标；对旋转矩形应使用局部有向Rect或等价的有向代理，不把现有混有标签的Group直接做世界轴非均匀缩放。
标签、入口/服务点符号、道路和控制柄本身不随尺寸缩放。控制柄屏幕尺寸稳定，不写进map.nodes。
若纯控制柄算法比适配当前渲染结构更小且正确，可以选择它，但只做所需矩形算法，不写通用变换框架。

## B. 不规则多边形交互

单选非矩形设施/区域后显示实际顶点控制柄，可拖动单个顶点；沿用已有自交、闭环、方向、孔洞与面积校验。
拖动是预览，松开才提交；无效修改回退到原有效几何并显示原因。不要靠删除孔洞、自动反转原数组或跳过校验来让测试通过。
本轮不增加布尔运算、曲线、复杂吸附体系、多对象联合缩放。

## C. 事务、关联与保存

- 一次拖拽只产生一个撤销记录；拖动中不写地图正文、不每帧自动保存。
- Esc、取消、只读、切换对象或失去有效交互上下文时恢复预览；拖动开始后地图版本变更则取消旧拖动，不能提交陈旧边界。
- 单选时显示控制柄；多选仍保留原有整体移动，不新增多对象尺寸操作。
- 默认缩放/顶点调整只改变边界，节点、道路、入口及服务点的坐标/ID不变。现有“整体移动关联点”策略继续用于移动/旋转，不隐式套用为缩放。
- 边界变化导致服务点空间关系可疑时，沿用或补最小警告，不强行搬动服务点以掩盖问题。
- 保留未应用表单保护；编辑过程中不能发生画布与属性面板同时覆盖彼此。
- 数值输入应能完成鼠标控制柄的等效操作；现有保存/导出/重开继续读取同一份领域几何。

## D. Ponytail 精简边界

本轮只做小步行为保持重构：复用重复枚举/工具函数，整理与此编辑链路有关的重复逻辑；没有明确收益就不移动文件。
不要为了减少行数删除校验、UTF-8/JSON错误处理、数据恢复、文件冲突保护、稳定引用、现有测试。
不要把复杂度从App搬进一个更泛化的“万能控制器”。必要时抽出一个专用控制柄组件/局部钩子即可。
README改成一个明确的当前产品入口；历史计划标为参考、不自动执行，不必删除历史记录，也不再每个小功能新增多份阶段文档。
不新增框架、不切换技术栈、不强行压成一行、不把整个项目改成一个HTML。

## 验收：复用现有测试体系，聚焦新增行为

1. 新建60×30矩形，拖角得到80×45；对角不动、四角仍直角，区域与设施均覆盖。
2. 旋转30°后拖角仍为矩形；尺寸在局部坐标中正确，标签不被缩放。
3. 缩放/平移视图前后做相同世界尺寸调整，最终JSON几何一致。
4. 普通多边形的单顶点可编辑；自交、零面积等提交失败时地图和历史不变。
5. 百个指针中间帧仅一个撤销事务；撤销重做正确，Esc不提交。
6. 保存、刷新、导出重入后边界、ID和引用一致；0.1.0/0.2.0旧矩形及不规则样例不被隐式迁移/改形。
7. 缩放建筑默认不移动或复制关联节点与道路；只读、多选、输入保护不被绕过。
8. 控制柄未被写入map.nodes；浏览器临时scale没有进入权威JSON；鼠标和数值入口共享几何结果。

先运行针对性回归，最后按实际package.json运行schema:check、typecheck、lint、test、test:integration、build、test:e2e。不能运行的检查明确标未运行。
交付实际代码、必要测试和一段实际变更说明。记录重构哪些重复、为什么保留哪些保护；不设减少行数百分比目标，不凭估算声称已简化多少。
只有网络文件读取不等于完成代码重构，只有声明使用Ponytail不等于插件已安装。
