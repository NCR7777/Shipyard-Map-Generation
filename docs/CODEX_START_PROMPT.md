请在当前仓库实施 FAST01，完整合同见 FAST_TRACE_SPEC.md、CURVES_AND_EXCHANGE.md 和 ACCEPTANCE.md。
先核对本地HEAD/工作树与已读基线bcf4ac4c后的变化，读取AGENTS.md和已有工程规则，不回退、不重复已经实现的BG01、RF01、SV、TE、MQ和UX改进。

我的新目标是：我只对照底图快速画路网、建筑和区域大轮廓；软件带可修改默认值，具体用途交给Codex批量推测；道路按实际米制宽度显示并可拖宽；直线和曲线能混接、路由和按弧长回放；以后导入外部调度过程和结果显示。

本次明确授权这些功能以及必要重构，更新CURRENT_SCOPE/README中与此冲突的旧阶段限制。仍不建设内置调度求解器、仿真引擎、3D/VR或在线AI后台。允许改写重复难维护的绘制/曲线代码，不为重构而换框架或重建历史、保存和主数据。

按四个可运行里程碑推进：
F1 无表单描图：自动ID/默认属性、两点画路、粗类轮廓、明确接路、拖边调宽、正常保存。
F2 真曲线：单一line+cubic几何、Schema升级、弧长/投影/切分、曲线中接路、转向/资源引用、同核显示和路由。
F3 批量补标：导出底图上下文和对象裁图，接收限字段语义补丁；生成可批量接受的运输目标接入建议和外部研究输入。
F4 外部结果：本地plan/events/summary导入，沿曲线弧长回放、等待状态、时间条和有口径的结果对比。

已有旧图不自动补默认属性；新默认为设计假设；手工几何和实测/未知原值不被模型擅改。输入未分类不阻止继续描图。不是每个操作都弹确认；只在数据丢失风险、未知引用影响或真实语义歧义时确认。

禁止只用Konva tension圆滑画面，却继续按旧折线计算。禁止为曲线采样建立密集nodes。禁止点路相交就全图自动连接、默认覆盖旧禁转、猜承载和净高。不要清空依赖来消除报错。

一个手势一次事务，普通编辑框架不变；预览不每帧深克隆/全图验证。只在输入边界、提交与发布进行必要检查。不要另造权限平台、事件总线和无限报告文件。

请先给一页短计划，随后实际实施并持续完成本次授权范围；每个里程碑保持应用可运行。仅在确实缺数据或需要不可逆/语义裁决时集中提出，不要每个按钮都停止等待。

在原编辑器用真实底图执行无表单标注、直曲混接、宽度调整、保存重开和外部示例回放。UI_DEMO.html只是交互参考，不是项目实现，不允许用其测试替代验收。
报告真正运行的命令/退出码、T01—T20状态、性能与标注步骤变化、剩余限制；保留原件，不改冻结SHA，未经要求不推送main。
