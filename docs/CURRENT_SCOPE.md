# 当前范围：FAST01 快速描图与外部结果展示

2026-09-15 从本地 `codex/rf00-rf01` 的 `ee1ae27d040876b40ba1edc8df314f2ca56cdba6` 继续实施 FAST01。该提交的 `map` 子树与规范核查基线 `bcf4ac4ceefedfcbbc769f694211acacb98542b8` 相同。四份本轮任务文档实际位于 `docs/`，不是 `docs/fast-trace/`。

本轮授权按 F1—F4 连续推进：无表单快速描图、统一 line/cubic 曲线几何、Codex 语义补丁与研究输入、外部计划/事件/指标的本地文件回放。用户负责几何描绘，软件提供可修改默认值和显式连接，外部程序负责调度求解。默认宽度与转向为设计假设；分类推测不改变几何、真实承载/净高或产能。

这项授权替代旧文档中“止于 RF01”及禁止外部任务回放的当前阶段限制。BG01、RF01、SV、TE、MQ、UX 的已实现部分继续复用；历史报告保留，不当成本轮通过证据。仍不建设内置求解器、仿真引擎、实时控制、3D/VR、在线 AI 后台或第二套地图、历史、存储。

原地图、底图、冻结 SHA 及用户既有修改保留。普通缩放与显示不改米制几何。业务动作通过既有事务入口，保存继续使用原控制器。外部补丁与结果绑定实际内容版本。实际进展和验收状态见 [FAST01 执行记录](FAST01_EXECUTION.md)，计划不等于完成。
