# M2A.1 独立验收计划与范围

依据：`Shipyard_M2A_Review_Service_Nodes_Next_9244a61.md` 第 9 节。本文件记录验收设计与实际执行结果；仅将本批已执行且通过的范围列为通过。保持 M1/M1.1/M2A 的全部原断言，兼容性测试显式构造 0.1.0，新增契约测试使用正式新版本。

## 本轮冻结基线

2026-09-09 在 `paper01/map` 实际顺序执行，未重装依赖，也未操作原 5173 服务。

| 命令 | 退出码 | 本轮实际结果 |
|---|---:|---|
| `npm.cmd run schema:check` | 0 | 生成类型一致 |
| `npm.cmd run typecheck` | 0 | 应用与无 DOM 核心 |
| `npm.cmd run lint` | 0 | ESLint 与 Core boundaries PASS |
| `npm.cmd run test` | 0 | 10 文件、170 项，3.05s |
| `npm.cmd run test:integration` | 0 | 2 文件、17 项，5.97s |
| `npm.cmd run build` | 0 | 209 modules；JS 802.41 kB、gzip 246.47 kB；保留大于 500 kB 提示 |
| `npm.cmd run test:e2e` | 0 | Chrome 28 项，1.2m |

证据保存于本地 `.cache/M2A1_baseline_results.json` 和 `.cache/M2A1_baseline_*.txt`。这组数字仅作为本轮改动前基线，不代替新增功能验收。

## 逐项映射

| 任务书编号 | 本批独立验证 | 边界 |
|---|---|---|
| 1 | 原始 M1/M2A JSON 在严格 0.1.0 下加载、稳定序列化与 ID 保留；显式升级可撤销 | 不自动写新字段到旧 Schema |
| 2 | 真鼠标选已有节点给设施添加 unloading 服务点，保存刷新保持 nodeId、设施归属、能力 | 不将普通节点自动升级为服务点 |
| 3 | 专用节点与服务点同一事务创建；失败无残留；一次撤销和稳定 ID 重做 | 绘制中仅为暂存数据 |
| 4 | yard 设施沿用服务归属；独立 Zone 使用正式字段和版本 | 不转换 Zone 类型 |
| 5 | 重叠区域仅用户指定者成为主归属；可以显式改归属 | 点落入多边形不产生主归属 |
| 6 | 水域、禁止通行区域维持原 kind/passability，特殊业务需明确声明 | 不生成陆地通行许可 |
| 7 | 区域移动策略、成员复制、删除闭包与共享节点影响；失败地图和历史原子不变 | 不隐式复制整个外部路网 |
| 8 | 悬空服务点 nodeId 的导入和命令报错定位到 JSON Pointer | 网络发布仍未实现 |
| 9 | 几何中部点无道路端点引用时显示未接路，显式拆路后检查入射道路引用 | 局部接路不等于完整可达 |
| 10 | 方向错误、必要转向未知及缺路不能报告已发布或已验证可达 | 完整转向/网络可达性归 M3A |
| 11 | 存储显式边界代理与假设；不自动生成室内道路或中心装卸点 | 合法任务路径编译归 M3A |
| 12 | 内部显式语义保留声明接续；缺接续提示，不按同设施瞬移 | 不宣称完成运输执行验证 |
| 24 | 原保存、双标签冲突、原生 API 故障与 OPFS 回归全部保留；新服务点刷新恢复 | OPFS 选择器替身不等于系统权限手工验收 |
| 25 | 属性/服务表单切对象或工具的取消保留、明确放弃；保存只确认已提交地图 | 不把暂存输入冒称已保存 |
| 26 | 双版本 Schema 严格性、生成类型、显式升级差异与来源/合法元数据/资产/扩展完整保留 | 未支持高级实体继续门禁保护 |

第 13–23 项为 M3A 测试计划，本批不实现到达—服务执行、资源竞争、货物状态机或事件回放。第 10–12 项仅验证本批数据契约与保守提示，完整路径合法性和运行结果不能判为本批完成。

## 测试组织

- `tests/unit/M2A1_serviceTargets.test.ts`：无 DOM 契约、迁移、所有权、命令闭包及引用校验；有效预期独立计算，不以被测函数自己验证自己。
- `tests/integration/M2A1_cli.test.ts`：真实命令行同时接收两版地图，错误字段定位与退出码，不跳过公共导入校验。
- `tests/e2e/M2A1_serviceTargets.spec.ts`：真实画布定位、已有节点绑定、专用节点事务、归属修改与暂存离开保护，保留原 28 项完整回归。
- 示例和 fixture 明确 synthetic；未定义服务时间、车速、资源容量和仿真事件，不补虚构运行数据。

保留首轮失败输出、截图、trace 和修复依据。只有在运行源码冻结后由唯一浏览器 runner 启动 4178；所有实际结果在最终阶段报告记录。

## 本轮实施后的实际结果入口

本计划已执行：最终230项单元、26项CLI集成、36项Chrome浏览器用例通过，Schema/类型/lint/构建退出0。首轮浏览器35/36及新增确认流程的旧测试修正、中断运行和最终完整重跑均在 [阶段报告](M2A1_STAGE_REPORT.md) 与 [结构化记录](M2A1_VALIDATION_RESULTS.json) 登记。上述改动前170/17/28仅作本轮基线保留，不计作新增验收。
