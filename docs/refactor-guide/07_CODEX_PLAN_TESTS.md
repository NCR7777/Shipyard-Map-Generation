# 07｜Codex实施计划与验收

## 1. 执行规则

逐阶段交付，不要求一次大改完成全部。每阶段只维护一个短执行日志：实际变化、测试命令/退出码、截图/trace、未完成/阻塞、下一阶段。共享Schema/命令/保存链同一阶段由一个集成责任方修改，审查者只读，避免多个代理同时重写App。
不自动推送main；不覆盖未提交工作；不写用户原地图。遵守当前AGENTS/Ponytail的纯核心、安全、兼容规则。先把本次扩大范围与历史暂停范围的差异写入唯一CURRENT_SCOPE或等价现有文档，不大规模修改历史报告。
不允许把“已写文档/已添加按钮/测试跳过”作为功能完成。

## 2. 分阶段工作单

### RF00｜冻结与当前状态（最先执行）

- 记录本地HEAD/分支/工作树，与6f8b0be差异；用本地最新源码复核本任务的F01–F15。
- 读取当前AGENTS、schema、GA01/TE01/DP1、测试配置和已有外部合同；旧修复已完成则仅回归。
- 定位18个目标原件及历史SR03/SR02，校验冻结SHA。缺件不修改manifest或假造同名图。
- 跑基线；固定错误、缺输入和环境失败分别记录。
- 录制三个当前任务：画直路/精修设施/配置交接；在1366×768、1920×1080、1093×614截图测量画布与错误行为。
- 输出需求能力矩阵与最小可运行纵向切片计划。完成后进入RF01，不在基线阶段无限写方案。

### RF01｜工作台、状态与保存交互

交付顶层菜单/上下文工具条/可调侧栏/底部抽屉；项目状态、当前工具、选择属性可一眼辨认；原菜单可过渡保留但不长期存在两套交互。
拆分App职责、统一事件与草稿/对话框模型；全部操作仍走旧domain/session。
先迁移最小链：导入真实图→搜索定位→已有可批准几何编辑→一次撤销/重做→保存→刷新→导出重导入。
把Ctrl+S和按钮合到同一保存入口，但保持浏览器/文件分目标确认。未应用输入可应用后保存、仅存已提交版、取消。
验收重点：选中对象/弹出警告不改变画布尺寸；模态焦点、input快捷键、冲突恢复不退化。

### RF02｜直接绘制与拓扑操作

完成框选、多选、重叠选择、Space临时平移、精确输入、两点直路、连续折线模式、道路形状手柄和上下文命令。
复用TE01拆/并/连/删引用重映射；复杂操作影响预览清楚。新增节点只为连接/服务/物理属性分段，不均匀采样。基础测距/面积在本地米制下只读。
将已有拓扑候选处理统一为Preview→Confirm→Commit，过期候选与取消无副作用。
验收：简单直路与高级图中新建简单独立道路确实可用；连接旧路必须显式；双向内部路径/共享资源/保留ID正确。

### RF03｜底图与工程包

把校准轻量栅格接入现有assets/backgroundLayers；加载、锁定、透明度、调整、来源、保存恢复与ZIP往返一次做闭环。
提供旧交付ZIP选择/迁入流程；接近或超过当前10 MiB上限的map，以及多百MB的TIFF，都不能通过删除限制或把影像base64塞入map来解决。
先支持可靠预处理轻量图，原生大GeoTIFF按窗读取可保持明确待办；影像变化不自动变换矢量。
验收：正常/缺资源/非法路径/超预算/多map/外部文件冲突/撤销后资源仍在。

### RF04｜设施、服务、路口、槽位、资源完整操作

补齐已知高级图上的新增、所有者内几何维护、服务点重绑/内部路径、厂界、资源配置、储位生成调整、逐转向矩阵。
不是一口气支持任意组合；按04_FEATURES定义的常用对象闭包逐项实现。对每类对象维护“创建/查看/编辑/复制/删除/发布”状态，列明支持条件。
业务向导优先画布拾取和连续候选；所有物理假设记录来源。高级依赖正常修改可完成，不能把命令永久blocked当作该需求已交付。
验收：真实地图至少一条生产设施→服务→公共道路→目标服务的修改链，含撤销/保存；储位重排、资源共享、公共固定点与内部路径正确。

### RF05｜诊断修复、可信统计与交接输出

从只读报告升级为“诊断→定位→已知修复预览→命令→重新检查”；unknown/not_checked保留。
路径预览/临时封路/数据适配筛查与渲染共用同一mapHash；大型诊断按测量引入Worker，不让旧结果覆盖新项目。
提供数量/去重面积/长度/单位分组容量；输出PNG/SVG与CSV/JSON，精确与估计口径区分。
验收：有禁转但节点可达的反例、第三方内部路线捷径反例、混合单位、视图隐藏不影响检查、异步过期结果。

### RF06｜外部调度合同与可运行消费者

实现06文档的输入编译/发布配置、唯一哈希、场景/计划/运行状态验证；复用或显式升级既有适配合同。
交付Node CLI＋Python读取示例＋本地mock结果往返；没有实际系统协议时不擅建服务器/优化器。
验收：无DOM读取真实地图，未知字段/缺参数阻塞可解释，旧计划hash失配，乱序状态处理；结果层不改变map。

### RF07｜完整用户任务、回归与交付

在同一最终源码/构建上完成真实18图及故障/性能验证。用户从普通入口走完打开→参考影像→改图→服务配置→检查→保存→导出发布，不得全部靠测试后门调用命令。
整理最新操作指南、快捷键/保存目标/来源/限制与接口合同。清理冗余旧UI入口，不删历史证据与必要保护。
外部调度器不在此阶段实现，正式现场验证也不是结束条件。用户可完成已约定研究制图和交接任务即停止扩张。

## 3. 测试分层与真实基线

保留快速单元、基础集成、浏览器、生产回归和真实输入门禁。通用CI可以有独立的self-contained夹具；真实数据验收命令必须要求冻结原件，不因缺件静默通过。
当前TE01报告记录unit 550通过+4缺SR02、integration86通过+4缺SR02；这不是本轮重跑结果。RF00重测后记录新基线，不沿用旧数字。[S07]
十八图V01/V02不是一个版本互相替代；同mapId时仍按路径和SHA独立。

开工时按实际package.json运行，示例：

```powershell
git status --short
git rev-parse HEAD
node --version
npm --version
# 首次安装采用锁定依赖；Node版本符合仓库声明，不能通过降依赖让测试跑起来。
npm ci
$env:GA01_DATA_ROOT = '<包含目标projects的真实根目录>'
$env:SHIPYARD_TEST_DATA_ROOT = '<包含历史SR02/SR03输入的真实根目录>'
$env:GA01_PHASE = 'B'
npm run schema:check
npm run typecheck
npm run lint
npm run test
npm run test:integration
npm run build
npm run test:e2e
npx playwright test -c playwright.topology.config.ts
npm run test:production
```

新的compile/publish/zip测试命令在实现时加入，不在未实现前伪造通过。每次公共契约、命令或保存逻辑变更必须覆盖受影响全量回归。

## 4. 必须覆盖的验收案例

下列编号用于追踪，不强制一行一个文件；尽量复用现有测试。每项状态为passed/failed/not_run/blocked_input，不能只用一个总布尔值。

| ID | 案例 | 必须观察的结果 |
|---|---|---|
| T01 | 18份原图打开 | SHA匹配；不改原件；不支持的对象仍有明确目录说明 |
| T02 | 普通几何编辑 | 整个coordinateFrame不变；只变明确字段和来源/版本 |
| T03 | 0.1/0.2往返 | 不隐式升级；键格式变化不改变数组次序或数值 |
| T04 | 取消/失败/no-op | hash、revision、历史和存储基线不变 |
| T05 | 跨项目旧草稿 | 旧token拒绝，不污染新图 |
| T06 | 全部目标窗口尺寸 | 无工具遮挡/页面横溢；正文可读；画布稳定 |
| T07 | 模态/文本/IME | 焦点正确；文本撤销删除不改地图 |
| T08 | 选择、框选、多选 | 选择与查看不改图；隐藏/锁定策略明确 |
| T09 | 重叠实体 | 可选择底层节点/服务/道路，不靠运气命中 |
| T10 | 平移缩放/快缩后点选 | 相机命中一致；Space释放/失焦恢复 |
| T11 | 两点直路 | 只创建必要端点和一条线，不留半成品 |
| T12 | 正交/角度约束 | UI度↔模型rad正确；坐标吸附不接路 |
| T13 | 道路落点接旧路 | 显式一次拆/连事务；取消不留节点 |
| T14 | X/T/近邻/重叠 | 四类区分；不同高度/未知不自动连通 |
| T15 | 正逆拆路 | 内部路径顺序及movement端点映射正确 |
| T16 | 拆路共享资源 | 保持同一resource ID/容量，appliesTo更新 |
| T17 | 二度节点 | 保持弯曲几何；必要服务/属性分段不删除 |
| T18 | 合并保留目标 | ID/坐标明确，目标锁定和新依赖也检查 |
| T19 | 删除复杂对象 | 预览引用和影响；无半删除；拒绝原因可定位 |
| T20 | 多选批改 | 混合值准确；不静默跳过失败对象 |
| T21 | 旋转矩形拖角 | 固定对角、直角、宽高独立；一次撤销 |
| T22 | 多边形/孔洞 | 自交/孔洞非法拒绝；不重排隐含语义 |
| T23 | 设施成组移动 | 内部内容联动，公共锚点按策略固定 |
| T24 | 缩小堆场 | 槽位/服务越界明确；不自动删掉 |
| T25 | 高级图新建设施 | 常见已知无冲突依赖可完成，不被全图门禁拦住 |
| T26 | 复制设施 | 内部ID重映射；外部共享资源不复制容量 |
| T27 | 创建/移动入口 | owner反向成员正确；服务路径起点一致 |
| T28 | 服务代理 | 说明与场景时长策略一致，不计为自动装卸 |
| T29 | 显式内部路线 | 顺序、方向、入口、终点一致；不瞬移 |
| T30 | 逐转向三态 | 允许/禁止/未知分别影响路径结论 |
| T31 | 改方向 | 冲突movement/internalPath可见，不隐式删除 |
| T32 | 储位变化 | 几何、owner、资源面积和来源一致，ID策略可解释 |
| T33 | 停车位 | 同owner且唯一parking服务点；不自动生成车队 |
| T34 | 资源容量/单位 | known/unknown/unrestricted/not_applicable不混用 |
| T35 | 未知行为扩展 | 无安全证明仍保护；完整保留载荷 |
| T36 | 底图校准 | 像素中心/角点、Y翻转、四角检查一致 |
| T37 | 底图重标定 | 只改背景，矢量坐标不跟着缩放 |
| T38 | 轻量图来源 | 原TIFF摘要和变换链可回溯，不重复原大图 |
| T39 | ZIP正常/旧交付包 | map选择正确、资产哈希保留、单JSON仍可用 |
| T40 | ZIP恶意或超限 | 不执行脚本，不覆盖当前工程，不无限解压 |
| T41 | 缺图片 | 矢量完整；不能宣称完整资源包 |
| T42 | 浏览器配额/权限失败 | 准确未保存；提供恢复/下载而不是假成功 |
| T43 | 原生文件被外改 | 冲突留双方，写前再读，不跨应用原子假承诺 |
| T44 | Ctrl+S/未应用输入 | 同主按钮流程；应用后保存、旧版本、取消各正确 |
| T45 | 多标签工程冲突 | 不静默覆盖新storageVersion |
| T46 | 隐藏/锁定后诊断 | 对象仍参与空间/路径检查 |
| T47 | 旧异步诊断/编译 | mapHash/project/token失配结果被丢弃 |
| T48 | 拓扑候选与确定结果 | unknown可能改变可达性时不报确定断开 |
| T49 | 第三方内部捷径 | 未批准内部通道不能作为公共最短路 |
| T50 | 面积/长度统计 | 物理道路不按双向计两次；多边形去重、孔洞扣除 |
| T51 | 发布缺必需参数 | blocked或有显式假设，未知不自动补值 |
| T52 | Node/Python消费者 | 无DOM读取，摘要/资源/服务可核对 |
| T53 | 旧计划套新图 | map/scenario hash失配拒绝，不暗自重绑 |
| T54 | 运行序号/epoch | 重复幂等、旧序号丢弃、重连语义明确 |
| T55 | 只读外部结果层 | 加载/卸载状态不改变mapHash或Undo |
| T56 | 最终构建复验 | 同源码同构建有命令退出码、截图、trace与剩余项 |

## 5. 性能验收目标（待本地基线校准）

在明确记录CPU/内存/GPU/浏览器/有效CSS视窗和固定最大目标图后，每类典型操作至少重复30次，长拖动/平移记录连续帧分布，并区分冷启动与热启动。不能拿小synthetic图代替最大真实图。
建议目标：常规选择/工具切换反馈p95≤100ms；拖动/平移稳定帧p95≤33ms；普通命令提交p95≤300ms；需要较久的诊断/编译100ms内给出忙状态并可取消。它们是设计预算，未在本轮实测。
未达预算先定位：React重渲染、命令候选深拷贝、重复全图验证、标签、命中、列表、图片解码。只优化证据中的瓶颈，不通过关闭安全验证造“流畅”。

## 6. 每阶段报告模板

```text
阶段：RFxx
基线/最终commit或工作树摘要：
输入：原件路径、SHA、是否齐全
实现：具体工作流与支持条件
验证：命令、退出码、passed/failed/not_run/blocked_input
人机交互证据：窗口尺寸、真实鼠标键盘、截图/trace
不变量：frame/ID/引用/来源/保存/未知值
剩余限制：明确到操作/实体，不写笼统“后续优化”
本阶段是否达到退出条件：是/否
下一阶段：仅在当前门禁满足后推进
```
