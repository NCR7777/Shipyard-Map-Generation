请以当前本地仓库最新HEAD为基础，实施“船厂地图编辑器全项目重构与功能补齐”。完整任务位于 docs/refactor-guide/（或我提供的同名指导包）。

先读取00_START_HERE.md、01_AUDIT_REQUIREMENTS.md、02_UX_SPEC.md和07_CODEX_PLAN_TESTS.md，再按阶段读取剩余章节。审查基线为6f8b0bea59e6277f4452d102a109ccacfedd3757；本地存在更新时先检查差异，不回退代码、不覆盖未提交改动。

目标不是更换UI配色，也不是重写全部代码，而是在保留现有技术栈、YardMap、命令事务、撤销、来源、保存恢复和固定coordinateFrame的基础上，得到操作顺畅、已知核心实体可编辑、支持底图与工程交换、能够向外部调度系统发布输入的工作台。

当前GA01固定地理锚点及TE01拓扑操作已有实现，不重复做旧修复。真正需要补齐的是任务导向界面、统一状态/保存交互、简单直接绘制、高级地图的具体依赖维护、服务/转向/储位/资源编辑、底图与ZIP、诊断修复，以及可执行的外部编译/交接合同。

按RF00→RF07实施，每阶段提供运行结果后再继续，不在一个巨型改动里混合UI、Schema与存储重写。现在先完成RF00和RF01的最小纵向链：真实地图打开→搜索定位→一个已批准的几何编辑→撤销重做→保存刷新→JSON往返，并按三个窗口尺寸验证新工作台。其余阶段依据指导包随后推进。

遵守AGENTS及现有Ponytail full/review，保留所有数据/输入/引用/冲突保护。普通编辑不得修改coordinateFrame，未知行为不得通过删字段绕过门禁。已知对象的常用编辑必须逐项实现正确依赖维护，不能把永久拒绝当作功能完成。

不修改十八份真实原件/冻结SHA，不上传影像或私有地图，不自动推送main。不在本仓库实现优化求解器、队列仿真、真实派工、3D/VR或微服务。调度接入先实现无DOM编译、声明发布配置、独立消费者和只读mock结果合同，不伪造真实接入。

先运行当前基线；SR02等缺件记blocked_input，不删测试或篡改原件。所有测试必须区分通过、失败、未运行和缺输入。原型HTML只作布局参考，不能作为实际编辑器或验收代替品。

每批只记录：修改、实际命令/退出码、鼠标键盘证据、剩余限制和阶段是否完成。不要停在仅写计划/按钮占位；也不要以无限“完善”为目标扩大范围。
