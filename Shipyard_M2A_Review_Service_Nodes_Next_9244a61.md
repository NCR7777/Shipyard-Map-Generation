# Shipyard-Map-Generation：M2A源码审查与服务节点续开发任务

日期：2026-09-09。
审查仓库：NCR7777/Shipyard-Map-Generation。
固定提交：9244a615d1689098592fba9b3abe393b9082047c（main，提交说明：完成M2A空间实体编辑与持久化恢复验收）。

## 0. 阅读方式与证据边界

这是源码审查与下一阶段设计建议，不是已实现的补丁，也不是本轮测试通过报告。通过 GitHub 连接读取了当前提交的命令、类型、能力门禁、界面关键函数、空间属性面板、持久化协调器、适配器、浏览器测试片段与阶段文档。没有修改或推送用户仓库。

本轮尝试在执行环境克隆仓库，因 `Could not resolve host: github.com` 失败，退出码128；因此没有独立执行 npm 安装、构建、单元测试或浏览器测试。不要将仓库报告的通过数字改写为本轮独立重跑结果。

仓库 M2A_STAGE_REPORT.md 记录170项单元测试、17项集成测试、最终28项浏览器测试通过；该报告同时说明原生系统文件授权/任意本地磁盘写回未完成手工实测。自动化OPFS/选择器替换测试不是原生文件选择器授权验收。

## 1. 本轮源码确认

### 1.1 已有能力，不要再从零实现

- `src/ui/useProjectWorkspace.ts` 使用 `IndexedDBProjectStore` 和 `ProjectController`；存在约600毫秒防抖草稿保存、恢复、项目切换保护和外部版本检查。恢复回调失败时暂停写入，避免旧画面写进新项目槽位。
- `src/domain/commands.ts` 已有六类选择、设施/区域创建修改、入口/服务点创建修改、整体变换、复制删除、道路拆分等命令。
- `src/ui/SpatialPropertyPanel.tsx` 有设施和独立区域的属性面板，并有入口/服务点的 nodeId、facilityId、accessPointId 编辑。
- `src/ui/App.tsx` 的 openPointDialog/createPoint 已提供添加入口、添加服务点，可选已有节点或创建专用节点；服务类型已含装载、卸载、停车、泊位、其他。
- `src/adapters/contracts.ts` 的 SceneSnapshot 已包含设施、区域、入口、服务点；服务点位置由 nodeId 派生，不单独保存另一组权威坐标。
- `tests/e2e/M2A_spatial.spec.ts` 有实际60×30米设施绘制、保存刷新、整体拖动、入口服务点创建等浏览器测试代码。存在测试代码不等于本轮已运行。

### 1.2 与本次需求直接相关的缺口

1. ServicePoint 当前支持 `nodeId`、可选 `facilityId`、可选 `accessPointId`、`resourceIds`，不支持 zoneId/区域归属。
2. Facility.kind 已包含 yard、assembly、dock、quay，不只包含实体建筑。独立 Zone 是通行/作业空间层，不能和 Facility 混称。
3. 选择独立区域并新增普通节点，只增加了空间对象；节点落在多边形里不自动建立业务归属。
4. 当前资源、junctions、movements、assets、backgroundLayers 仍在能力门禁中。手工插入资源记录可能使整图只读，不能仅删除门禁而跳过命令、引用、渲染、校验和测试。
5. 当前没有服务点之间的完整可达性/路径发布，也没有装卸事件执行器；SimulationAdapter 仍是接口，RuntimeStateMessage 主要承载位姿，不是已经实现的任务状态机。
6. 当前是单一 servicePoint.kind；同一物理作业位兼容装与卸、多个服务点共享同一作业资源，需要明确表达，不能通过复制点获得额外容量。
7. 未应用表单和未完成绘制不属于已提交地图；切换对象/工具时仍可能丢弃这些暂存输入。增加离开暂存编辑确认，比宣称所有输入都已自动保存更准确。
8. App.tsx 当前约56.9KB。后续宜拆出服务点编辑/工作区/运行面板，但这不是要求重写领域模型，也不是单凭文件大小判定程序错误。

## 2. 对“建筑/区域添加节点，到达后装卸”的设计裁决

允许，并建议用现有 Node + ServicePoint 实现：

```
道路节点 -> 明确道路/内部接续 -> 目标节点
                                  ^
                                  | servicePoint.nodeId
                              装卸服务点
                                  |
                        业务归属设施或区域
```

Node 回答“车辆在路网哪里”；ServicePoint 回答“此位置能够执行什么业务”。任务引用 servicePointId，路径终点由它解析为 nodeId。不要把所有普通节点都自动当作装卸点。

到达指定目标节点，可定义为到达对应业务地点；但到达、开始服务、完成服务必须是不同事件。车辆路过同址节点、达到等待区、到达同设施的另一个点，都不能误完成当前目标。

这是拟议的节点级离散事件抽象，适合第二层研究起步；不声称验证了真实车货扫掠、路口尾部清空、厂房室内运动或吊装动力学。

## 3. 所有权与区域：最小变更优先

### 3.1 当前版本可立即使用的方式

将有明确业务身份的堆场/总组区建为 Facility(kind=yard/assembly)，用已有服务点关联它。Zone 可以叠加表达等待、禁入、允许通行等局部空间规则。不要仅为了满足UI而把水域/禁区强制转换为设施。

### 3.2 用户确实需要独立Zone直接拥有服务点时

增加显式区域归属，不能依赖名称匹配或每帧point-in-polygon自动猜父级。

建议先设计并记录兼容方案：可以给 ServicePoint 增加可选 zoneId，并定义 facilityId 与 zoneId 的互斥主归属规则；accessPointId 属于设施入口时必须匹配相同设施，不能和区域主归属含混共存。空间覆盖的多个区域另作查询结果，不混入“主归属”。另一种方案是统一 ownerRef，但若采用必须迁移并删除双份权威归属，不能让旧字段与ownerRef独立更新。

默认优先小规模、可验证的增量方案。新增字段在当前0.1.0 Schema中不合法；必须更新Schema、生成类型、兼容读取/迁移、浏览器工程存储和测试。保留旧文件备份；不自动把已有独立服务点分给最近区域。未声明归属的服务点允许作为草稿，不伪装已完成业务绑定。

区域移动、复制、删除必须明确处理成员点；避免出现“区域搬走，装卸点悄悄留在旧位置”。共用节点及外部道路的影响沿用显式策略与预览。反向成员查询尽量由主归属派生，不为Zone增加一套未经约束的冗余成员列表。

## 4. 两种合法的到达抽象

### A. 边界代理服务点（建议首版默认选项，而非强制）

服务点设置在建筑门外、入口处或堆场边界的合法道路端点。到达该点即认为完成对该业务地点的运输到达；未建模的场内转运按明确假设纳入服务时长，或明确忽略并说明适用边界。

不要在未建室内道路时把目标点塞到厂房中心，再用直线穿墙路径补全。

### B. 内部显式服务点

公共道路 -> 出入口 -> 已绘制内部通路 -> 装卸节点。内部路径距离、旅行时间和资源占用需要实际计算，不能因设置了accessPointId就直接瞬移。

同一地图允许不同设施选不同抽象，但每个服务点的到达语义必须明确。不要同时在内部道路旅行时间和服务时长中重复核算相同内部转运。

若同时用入口和装卸点引用同一nodeId，必须视为用户明确选择的节点级简化，而不是生成两个不相连的重合节点。

## 5. 数据分层与拟议字段

以下是新增设计，不是现仓库已经支持的导入格式。

### map.json：相对稳定的空间与能力

- nodeId、设施/区域主归属、入口关联。
- 服务能力：装载、卸载、停车等；设计 supportedOperations 时保留旧kind迁移/显示兼容，不能产生两套冲突判定。
- 到达抽象：node_proxy / explicit_internal；必要的等待点引用与来源。
- 作业位、共享吊机等资源身份及名义容量、通行冲突关系；沿用现有 resources，不在每个点重复创建同一台设备。
- 所有权威位置仍在 nodes，图形半径只控制显示/命中，不作为仿真到达半径。

### scenario.json：一场实验的任务与运行设定

- schemaVersion、mapId、mapContentHash、scenarioId、随机种子。
- 任务originServicePointId、destinationServicePointId、货物、放行时间。
- 服务时长模型、来源与单位；首轮固定时长即可，标明synthetic，不默认“真实装卸5分钟”。
- 车辆初始状态、空载/重载速度假设、设备/班次日历、排队规则、运行期资源停用。
- 缺失运行参数时拒绝启动该仿真配置或要求显式假设；不得无提示补默认速度/时长/容量。

### runtime/events：运行后的状态与事实

- 车辆阶段、任务阶段、货物持有者/位置、资源占用与队列。
- 到达、等待、开始、完成、离开、失败等事件。
- 不把当前任务、当前占用、剩余作业秒数写入map.json。
- 图像动画读取位姿与事件，不决定业务事实。

## 6. 最小到达—服务状态机

```
TO_PICKUP
  -> ARRIVED_PICKUP
  -> WAITING_TO_LOAD (按需)
  -> LOADING
  -> LOADED
  -> TO_DROPOFF
  -> ARRIVED_DROPOFF
  -> WAITING_TO_UNLOAD (按需)
  -> UNLOADING
  -> COMPLETED
```

额外区分 ARRIVED_QUEUE / ARRIVED_SERVICE：有物理队列点时，车先到等待点；没有作业位许可不进入被占用作业位。逻辑队列抽象也可以用，但必须显式声明，不把无限叠车动画称作空间可行。

### 到达条件

- 当前任务/阶段的目标servicePointId匹配。
- 完成一条已编译为可行的路网路径，终止于目标nodeId。
- 节点级引擎以精确调度的事件时间触发到达，不依赖requestAnimationFrame是否恰好采样到坐标。
- 空路径只在车辆确实已位于目标节点且满足目标/阶段条件时允许。
- 可配置数值容差只能防浮点误差，不能穿墙、跨断路或替代路线可达性。
- 每个run/task/stage/arrival重复投递保持幂等。

### 开始条件

- 已到达合法服务/等待流程对应位置，操作类型允许。
- 货物可用、车辆载荷状态相符、任务已放行。
- 本阶段所需资源、服务位和日历满足；资源申请采用明确且不会双重占用的机制。
- 等待期间车辆所在节点/道路/缓冲区占用规则明确。容量不足时在合法位置等待或阻塞，不能默默从地图删除车辆。

### 完成条件

- 从service_started开始推进规定服务时长；是否可中断先固定为非抢占并说明工作窗处理。
- 到达不等于完成。装载完成才改变货物持有关系为车辆；卸载完成才改变为目标位置，并按本轮明确规则完成任务。
- 货物在任何时刻只有一个合法持有者；车辆不在服务未完成时启动下一运输阶段。
- 释放作业设备与车辆离开作业位分开建模；不能服务完成后释放全部空间资源，却仍将车永久留在作业位。

建议事件包含 runId、eventId、sequence、simulationTime、taskId、vehicleId、servicePointId、nodeId、operation、stage；保留occurredAt/observedAt，默认可相同，只有声明消息延迟场景时才分开。

## 7. 接入路径与发布边界

先实现无DOM的服务点解析/可达性检查。遵守道路方向与声明的转向连接，不把同址不同ID节点自动合并，不把折线交叉自动连通。

不能直接因当前没有movements，就默认所有路口所有转向都合法。可以允许用户明确选择并编译一套简化转向策略，也可以要求显式填写转向；两者需可辨认，不能隐藏假设。

“给服务点绑一个入口”只是业务引用，不能充当入口到服务点的路径。道路经过设施边界时，需要区分边界代理、合法入口和已声明内部通道；禁止简单以设施多边形一概拒绝合法服务通路，也禁止无条件穿越所有设施。

先做单车单任务、固定服务时长的无渲染运行测试，再做双车共享一个作业位。暂不实施优化调度、学习算法、3D或VR。渲染只是可选回放，同一场景有无浏览器、不同播放速度应生成同一事件序列。

资源能力门禁应逐步、按实际实现能力解除；不得以取消所有readonly判断获得“运行成功”。资源引用、删除/复制、Schema、编译、运行占用和测试要同时接通。

## 8. 推荐分阶段交付

### M2A.1：目标服务点易用性与契约（下一次主任务）

- 沿用已实现的浏览器保存、空间命令和入口/服务点，不重复搭建。
- 选设施或合适的业务区域 -> 添加到达/服务点 -> 在画布点选已有节点，或创建专用节点 -> 选择装载/卸载能力 -> 关联业务对象。
- 目标点显式接路；已有拆路命令复用，提供清楚的缺连接提示。
- 决定并实现区域归属兼容方案；区分设施yard与独立zone。
- 明确边界代理或内部显式路径；暂不实现完整仿真引擎。
- 增加暂存表单离开提醒。

### M2B：真实布局依据（独立后续批次）

底图、本地资源、标定、ZIP工程往返。与服务点契约相容。当前是缺口，不能因演示车能跑就宣称地图接近真实。

### M3A：网络发布与到达—装卸最小验证（M2A.1之后）

- 服务点解析、道路/转向可达性、必要资源编译、scenario契约。
- 无DOM的单任务执行，固定时长；再添加两车共享一作业位测试。
- 当前引擎不支持的物理约束明确为unsupported/unchecked。草稿保存与运行发布分开。
- 事件日志+简单回放面板，不以帧计时实现装卸业务。

初期地图可以是明示synthetic的很小路网。服务语义与可达性先验证，不要求把真实大厂全部画完再运行测试。

## 9. 验收用例（按批次实施）

1. 现有M1/M2A JSON继续可读，旧设施和节点ID不被重新生成。
2. 建筑通过已有节点添加卸载服务点，保存刷新后归属、能力和nodeId保持。
3. 新建专用服务节点是一个可撤销事务，不产生没有归属的残留半成品。
4. Facility(kind=yard)可使用现有服务点；独立Zone新归属经正式Schema和兼容处理，不靠名字猜测。
5. 重叠区域不能自动同时成为主归属；用户选择必须可读且可修改。
6. 水域/禁入区不会因添加服务点就变成普通陆上通行区；特殊业务需明确声明。
7. 设施/区域移动、复制和删除按已选择策略处理服务点，所有依赖有检查，失败不破坏原图。
8. 服务点nodeId不存在，导入/发布定位到该字段并拒绝。
9. 点落在道路几何中部但未拆路时，不认为已经连通；显式拆路之后可检查连接。
10. 禁止方向、未声明必要转向、断路、无内部接续均不能生成伪可达路径。
11. 边界代理服务点可合法作为目标，无需为了通过检查强行绘制厂房室内路线。
12. 内部显式服务点必须具有接续路径，不因归属同一建筑而瞬移。
13. 经过目标节点但当前任务目标是另一个servicePoint，不能触发错误装卸。
14. 到达事件只触发一次，重复消息不重复申请资源或增减货物。
15. 服务时长未结束时任务不完成；等待时间与作业时间分开记录。
16. 空车不能卸不存在的货；装载完与卸载完的货物守恒。
17. 两辆车共享capacity=1资源时不能同时服务；两个同址服务点共用同一resourceId也不能获得双倍容量。
18. 在纯服务资源单元测试中，固定到达请求时间0/30s、服务时长120s、capacity=1、FCFS、忽略转场且明确为synthetic，应得到开始时间0/120s、完成120/240s。该测试验证排队语义，不证明空间队列物理可行。
19. 空间运行测试必须有合法等待点/阻塞规则，不能把18的零转场逻辑队列直接冒充真实停车布局。
20. 设备不可用或工作窗不足时不能开始不允许中断的服务；可重试或失败原因明确。
21. 关闭渲染器、加速回放、切换标签页不改变业务事件时刻与结果。
22. 同一map/scenario/seed生成确定性事件序列；运行中地图变更要求新版本/重启，不改写历史。
23. 静态map.json不因运行而混入车辆当前状态和队列计数。
24. 新服务点与运行功能不破坏现有自动保存/多项目冲突保护；文件写回失败不显示成功。
25. 未应用属性切换对象/工具时确认或保留草稿，保存消息不冒称未提交字段已保存。
26. Schema与生成类型一致；迁移不静默删除未知合法元数据、来源和资产引用。

## 10. 可直接发送给Codex的启动指令

请读取本文，并先固定当前仓库提交与基线差异。保持既有M1.1保存和M2A空间编辑。下一步不是再创建一个画图工具，而是将现有Node + ServicePoint贯通为建筑/业务区域的可用运输目标。

本次先实施M2A.1：服务点画布定位、已有节点绑定、设施/区域显式归属、边界代理/内部显式语义、接路提示、兼容方案与测试。先用现有设施kind=yard支持堆场；用户选择独立Zone时按正式Schema设计归属，不暗中转换对象类型。不要把所有普通节点都升级为装卸点。

说明M3A数据契约与测试计划，但不把未运行的到达/装卸流程标为完成。完成本批后，再分任务实现M3A的无DOM节点级到达—服务执行、资源竞争和事件日志。底图/ZIP仍按M2B独立实施；本次不做3D/VR和复杂优化调度。

所有变更走已有命令与事务、兼容保存/导入/CLI校验。不能简单删除capabilities门禁来绕过尚未实现的资源/转向行为。任务目标引用servicePointId，位置由nodeId派生，到达不等于服务开始/完成。实现中的服务规则是声明的研究模型，不标成现场实测。

实际运行schema:check、typecheck、lint、test、test:integration、build、test:e2e，补充本批适用验收。保留失败证据并修复；测试环境不支持时如实报告，不使用旧阶段通过数字替代本轮运行。不直接推送本任务以外的研究资料。

## 11. 固定源码引用

以下URL用于文件内追溯，全部固定到审查提交。外部论文不是判断该仓库已实现功能的依据。

- https://github.com/NCR7777/Shipyard-Map-Generation/commit/9244a615d1689098592fba9b3abe393b9082047c
- https://github.com/NCR7777/Shipyard-Map-Generation/blob/9244a615d1689098592fba9b3abe393b9082047c/src/domain/commands.ts
- https://github.com/NCR7777/Shipyard-Map-Generation/blob/9244a615d1689098592fba9b3abe393b9082047c/src/domain/model.generated.ts
- https://github.com/NCR7777/Shipyard-Map-Generation/blob/9244a615d1689098592fba9b3abe393b9082047c/src/domain/capabilities.ts
- https://github.com/NCR7777/Shipyard-Map-Generation/blob/9244a615d1689098592fba9b3abe393b9082047c/src/ui/App.tsx
- https://github.com/NCR7777/Shipyard-Map-Generation/blob/9244a615d1689098592fba9b3abe393b9082047c/src/ui/SpatialPropertyPanel.tsx
- https://github.com/NCR7777/Shipyard-Map-Generation/blob/9244a615d1689098592fba9b3abe393b9082047c/src/ui/useProjectWorkspace.ts
- https://github.com/NCR7777/Shipyard-Map-Generation/blob/9244a615d1689098592fba9b3abe393b9082047c/src/adapters/contracts.ts
- https://github.com/NCR7777/Shipyard-Map-Generation/blob/9244a615d1689098592fba9b3abe393b9082047c/tests/e2e/M2A_spatial.spec.ts
- https://github.com/NCR7777/Shipyard-Map-Generation/blob/9244a615d1689098592fba9b3abe393b9082047c/docs/M2A_STAGE_REPORT.md
- https://github.com/NCR7777/Shipyard-Map-Generation/blob/9244a615d1689098592fba9b3abe393b9082047c/docs/M2A_USER_GUIDE.md

外部实现参照，仅用于解释拟议设计边界：
- SUMO Logistics（停靠业务位置与装卸）：https://sumo.dlr.de/docs/Specification/Logistics.html
- Codex Best Practices（阶段任务与实际验证）：https://developers.openai.com/codex/learn/best-practices
